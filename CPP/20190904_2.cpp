#include <iostream>
using namespace std;

int main() {
	/*
	// 섭씨 온도 구하는 프로그램
	double f, C;

	cout << "화씨온도입력 : ";
	cin >> f;
	
	C = 5.0 / 9.0 * (f - 32.0);

	cout << "섭씨온도 = " << C;

	return 0;
	*/
	
	/*
	//3개의 정수를 입력받아 가장 큰 수를 출력하는 프로그램
	int a, b, c, big = 0;
	
	cout << "3개의 정수 입력 : ";
	cin >> a >> b >> c;

	if (a > b) {
		if (a > c)	big = a;
		else big = c;
	}
	else {
		if (b > c)	big = b;
		else big = c;
	}

	cout << "가장 큰 수는? " << big;

	return 0;
	*/
}